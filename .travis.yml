language: python
env:
  - DOCKER_COMPOSE_VERSION=1.23.2


services: docker

before_install:
  - export USER_ID=$(id -u ${USER})
  - docker -v
  # Use the env config
  - cp env.sample .env
  # Stop the postgres instance travis sets up automatically
  - sudo /etc/init.d/postgresql stop
  - sudo service mysql stop

  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - docker-compose --version
  # - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"

install:
  - docker-compose up --build -d

script:
  - bash docker/utils/ci_script.sh
  - docker-compose logs

#after_success:
  # Deploy image
  # - if [[ $TRAVIS_BRANCH == '2019.2.0' ]]; then docker tag "latenighttales/alcali:$TRAVIS_BRANCH" "latenighttales/alcali:$TRAVIS_BRANCH-latest" && docker push "latenighttales/alcali:$TRAVIS_BRANCH-latest" ; fi
